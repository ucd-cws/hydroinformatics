{% extends "subpage.django" %}
{% block content %}

 {% if graph %}
    <h3> {{ graph.name }} </h3>
    <h1> {% url 'graphImage' graph.id as the_graph %} </h1>
    <img src="{{the_graph}}" alt="{{the_graph}}"/>
{% endif %}

{% endblock  %}